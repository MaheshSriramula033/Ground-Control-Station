events {}

http {
    include       /etc/nginx/mime.types;
    sendfile      on;

    server {
        listen 80;
        server_name _;

        root /usr/share/nginx/html;

        location / {
            try_files $uri /index.html;
        }
    }
}
